# requirements.txt
# Verified compatible with Python 3.14.3 (Windows)
# Install: python3 -m pip install -r requirements.txt
# After install: python3 -m playwright install chromium

# ── Data validation and configuration ─────────────────────────────────────────
pydantic==2.12.5          # Core data model validation for RawListing, EnrichedListing, FilterResult
pydantic-settings==2.13.1 # .env file loading with type coercion; wraps pydantic BaseSettings

# ── Async browser automation ───────────────────────────────────────────────────
playwright==1.58.0         # Chromium browser control for Facebook scraping with cookie injection

# ── OpenAI API ─────────────────────────────────────────────────────────────────
# NOTE: openai 2.x — chat.completions.create() API is unchanged from 1.x
openai==2.21.0             # Official OpenAI client; GPT-4o-mini filtering with JSON mode

# ── HTTP client (Telegram Bot API) ────────────────────────────────────────────
httpx==0.28.1              # Sync HTTP client for Telegram Bot API calls

# ── Structured logging ─────────────────────────────────────────────────────────
structlog==25.5.0          # Structured log output: JSON to file, pretty-printed to console

# ── CLI ────────────────────────────────────────────────────────────────────────
click==8.3.1               # CLI: --csv, --dry-run, --headed, --health-check, --daemon, --once

# ── Task scheduling (daemon mode) ─────────────────────────────────────────────
APScheduler==3.11.2        # Cron-style scheduling for --daemon mode; asyncio-compatible

# ── Retry logic ────────────────────────────────────────────────────────────────
tenacity==9.1.4            # Exponential backoff retry decorator for OpenAI and Telegram calls

# ── Environment file loading ───────────────────────────────────────────────────
python-dotenv==1.2.1       # Required by pydantic-settings internally for .env file parsing

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==9.0.2              # Test runner for all unit and integration tests
pytest-asyncio==1.3.0      # Async test support; asyncio_mode="auto" in pyproject.toml
pytest-mock==3.15.1        # Clean mock fixtures; used in every test file
pytest-cov==7.0.0          # Coverage reporting; target >= 80% for all modules

# ── Code quality ───────────────────────────────────────────────────────────────
ruff==0.15.2               # Linter and formatter: replaces flake8, isort, black in one tool
mypy==1.19.1               # Static type checking

# ── Security (pre-commit) ──────────────────────────────────────────────────────
detect-secrets==1.5.0      # Scans staged files for accidentally committed secrets
